# Release Exporter API

An API to authenticate with GitHub and GitLab to export releases.

This API runs on Cloudflare Workers.

## Usage

Install the packages:

```bash
npm install
```

Run the tests:

```bash
npm test
```
